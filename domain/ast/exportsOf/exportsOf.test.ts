import { exportsOf } from ".";

describe("domain > AST > exportsOf", () => {
  it.todo("should return a transformer function");
  it.todo("should keep the root node in a given tree");
  it.todo("should keep a named import node if allowed");
  it.todo("should not keep a named import node if not allowed");
  it.todo("should keep a default import node if allowed");
  it.todo("should not keep a default import node if not allowed");
});
